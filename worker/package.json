{
  "name": "keranjang-pintar-demo-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply keranjang_pintar_demo --local",
    "d1:migrate:remote": "wrangler d1 migrations apply keranjang_pintar_demo --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
